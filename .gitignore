# IDE
*.iml
.idea/*
!/.idea/codeStyles
/.idea/codeStyles/*
!/.idea/codeStyles/Project.xml
!/.idea/codeStyles/codeStyleConfig.xml
.mcp.json

# OS
.DS_Store
Thumbs.db

# Gradle / Android build output
.gradle
build
/release
captures
.externalNativeBuild
.cxx
local.properties
*.log
*.hprof

# Node
node_modules

# Generated by `npm run prebuild` (phrasey) - never commit these
*.g.kt
*.g.json
phrasey-dist
app/src/main/assets/i18n

# Signing material. The keystore lives only on the release machine; see SIGNING.md.
secrets
*.jks
*.keystore
*.p12
keystore.properties

# Kotlin incremental compilation
.kotlin

# Release artefacts. `npm run android:move-outputs` writes named APKs/AAB here;
# they are published as GitHub release assets, never committed.
dist

# Retired hand-copied APK location, kept ignored as a safety net.
artifacts
